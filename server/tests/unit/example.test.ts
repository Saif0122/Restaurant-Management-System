import { sum } from '../../src/utils/math.utils'; // Assuming a math util exists

// For demonstration purposes, we will mock a simple utility test
describe('Unit Test Example', () => {
  it('should pass a basic assertion', () => {
    expect(1 + 1).toBe(2);
  });
});
